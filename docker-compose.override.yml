# Локальный оверрайд: на хосте уже занят 5432 (PostgreSQL 17 как служба Windows).
# Пробрасываем контейнерный Postgres на 55432, чтобы не конфликтовать.
# Внутри compose-сети сервисы всё равно ходят на postgres:5432.
services:
  postgres:
    ports: !override
      - "127.0.0.1:55432:5432"
